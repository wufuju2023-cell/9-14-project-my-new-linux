---

背景:alpha proof用的lean形式化环境和奖励还有mcts再加上蒙特卡洛树搜索和策略网络（LLM）+价值网络（可微value head）参数更新(ttt测试时更新)。可以把它分为两大模块，一类是CPU侧的模块，分别是lean mcts，另一个是gpu模块,包括策略网络,价值头和参数更新。研究CPU部分需要假设gpu部分已经被封装为CPU部分可用的接口反之同理


输入：根状态 s_0 = (C_0, Δ_0, E_0)，模拟次数 N_sim，温度 τ，探索常数 c_puct

For simulation = 1..N_sim:
# ===== 阶段 1：选择（Selection）=====
node ← root
path ← [node]
While node 已扩展 and node 非终止:
# 若当前节点是 tactic 决策节点，用 PUCT 选择
a ← argmax_{a ∈ A_tactic} [ Q(node,a) + c_puct · P(node,a) · √(Σ_b N(node,b)) / (1 + N(node,a)) ]
node ← child(node, a)
path.append(node)

```
# 若新节点触发工具条件，执行工具链（确定性）
    While should_call_tool(node.state):
        τ ← tool_policy(node.state)   # 启发式或独立策略
        node.state ← Exec_tool(node.state, τ)   # 确定性转移，更新 E
        # 不创建新 MCTS 节点，原地更新状态

# ===== 阶段 2：扩展（Expansion）=====
If node 未扩展 and node 非终止:
    (p(·|node), v(node)) ← GPU_Forward(Prefix(node))   # 策略+价值网络
    For each a ∈ A_tactic:
        创建子节点 child(node, a)，初始化:
            N(child) = 0, W(child) = 0
            P(child) = p(a|node)
    # 工具动作不创建子节点，由 should_call_tool 处理

# ===== 阶段 3：评估（Evaluation）=====
If node 终止（Δ ≡ ⊤）:
    z ← +1
Else:
    z ← v(node)   # GPU 价值网络输出

# ===== 阶段 4：反向传播（Backpropagation）=====
For n in path:
    N(n) ← N(n) + 1
    W(n) ← W(n) + z
    Q(n) ← W(n) / N(n)
```


# ===== 根节点策略提取 =====

π_MCTS(a | s_0) ∝ N(s_0, a)^{1/τ},  a ∈ A_tactic

# ===== 参数更新 =====

θ_i ← θ_i - α ∇_θ [ -Σ_a π_MCTS(a|s_0) log π_i(a|s_0; θ_i) + λ_v (v_i(s_0; θ_i) - z)^2 ]

Q0:在外部的教师调用者来看,如果每个子alpha proof，它版本非固定的话，也未必是好事,因为我们希望提高阿尔法proof本身的能力，我希望还是能够有经验继承，若产生大量的不同版本的alpha proof，估计也意义不大.但问题就在于,还执意采取传统的单智能体框架,势必无法让他同时维持一致的版本.这使我想起以前我学到的多智能体强化学习,在这里，这里头它是有一个状态向量和策略向量，包含多个状态和动作，还有一个联合奖励函数，这样子一次这个梯度下降能更新多个，这似乎是能够让他们形成一个更大型的系统，但是好像每一个子alpha prof来说，每一个版本依然不同了。也许这种分支和版本管理并不是真正重要的，或者可以借鉴遗传算法，比如说阿尔法evolve的思路来处理版本问题吗？或许在多智能体强化学习框架下，本身就把它当成一个整体的话，可能也没那么重要了。可能有些子阿尔法proof被弄得擅长解几何题，有些被弄得擅长解微分方程的大，但他们在同一个多智能体强化学习环境里，应该也能够把整个问题统统解决更难问题直接贡献吧。多智能体强化学习中，你每次都入一个问题，就是他会给我一个联合环境，然后，但此时，这个问题可能就是一模一样，或者说你也可以给你的多智能体分配多个问题，然后让传统大模型来调用，此时，他们就会获得一个联合奖励，然后根据这个联合奖励再同时更新每一个子alpha proof的参数。也就是说，应该是他不管sub alpha proof在干嘛，只要联合奖励越大就越好。可能会引出某些问题，就是可能某些阿尔法proof恰好被弄得更强了一些，然后那些比较弱的可能贡献会变低，最后就会是一种低贡献被忽略的状态。但是可以刻意的对每一种阿尔法proof，让它在不同的问题上进行训练，然后在多智能体强化学习框架里用，这个框架它其实是比那个moe要灵活一些，但有类似moe的功效，而且moe要求还是同一个模型，而它可以是不同模型，这样子可以在海量的不同的显卡上进行分布式的训练。具体如何进行多专家的那种多智能体强化学习训练？

Q0-1:现在又有了一些疑问，首先就是说如果是多智能体强化学习专家个数是得设置多少，因为我看粗度过粗的话，可能专家数量太少的话也不太行，但是没多了的话可能又增加训练量，而且我又发现他这个跟传统moe又不太一样moe单次对话耦合更强。另外，主训练过程，他难道不是直接取大量专门领域的问题，比如说群论，环论，域论，伽罗瓦理论群表示，论利群理论代数几何，理论范畴论这些的问题，然后分别训练很多专家，是这样吗？是直接弄一大堆LoRA adapter吗？然后这些专家应该是放进整个多智能体的环境里头，然后他通过分配到多个子智能体，然后他们子智能体又进行mcts，然后获得奖励以后再梯度下降更新策略网络和价值头，这样子，每类问题取两k到10k的进行训练。还是说还需要引入传统的一些sft和rl方法

Q1:我再仔细探索一下q Lora,如果真的允许的话，我们直接就拿那个120b的千问3.8 flash做主agent，然后这个做子agent.如果失败了的话，就还选那个9b的，qwen 3.8 distill的做子agent 27b做主agent。基座模型越强就越没有冷起动的问题，越能减少我们的训练负担。但是越大的模型需要的训练数据也越多，训练数据少了不容易引起显著的行为定向改变。

Q1-1:哎呦，那还真是这样子。那看来得权衡一下了。可以先找些冷门一点的领域。某些范畴论之类的东西，他大概就做不太了。但是这个需要做数据，比如说把higher topos theory和higher algebra,kerodon先翻译成lean tree,还有lean里头中的范畴论内容.这些他本来大概率就搞不出来.但是翻译这个东西要耗很多token.那几本书每个都有1000多页.这样子一通弄下来可能几百亿token就没了.不过我听说现在的agent翻译这些似乎还是能做到.

Q1-2:会有两个环节会出现opencode的这样子的环境第一个是多智能体强化学习，作为外部调用者，他可能是一个大模型，然后他来调用那些子智能体阿尔法proof.然后对于那些子阿尔法proof本身，他们的策略再从gpu采样过程到生成单个节点的tactic时，也是过了一轮open code，这个过程中相当于增加他们这个采样后策略的这个能力允许他们进行工具调用lean, search ,lake build,类型检查，撰写脚本实验之类，但是所有随机性还是来源于这个策略采样.

Q1-3:在AMD MI300x 192 GB显存上，能否高效的用lora或者qlora使用多个adapter微调qwen3.8 27b模型，我有单卡，还有多张独立的，这样子的卡，但是他们之间没有多卡互联.  是要多智能体强化学习,会有多个子智能体，并且他们会受到形式化环境的奖励,根据输入的问题，有联合奖励函数和联合策略.或者说某种长得像moe一样的Lora多个adapter作为他们的这个子智能体或者专家

Q2:我又想到一个事，就是关于这个采样的多样性，有策略真的能够给出多种本质上不同的解答吗？因为如果是对围棋的格局，他明显可以确保两种是完全不一样的，但是，策略采样出来，那些文本可能只是表述不一样，本质也差不多。况且，原先很多基座大模型，他采用grpo或其变体训练，提高他一轮的正确率。但是，多轮采样的多样性不知能不能保证，而且多样性不够的话mcts很难训练下去，Mcts总是假定，你总会留于概率原因，恰好会使得某些能够证出来，某些都证不出来，但是会不会有全部停留在一个孱弱的，很低洼的这个奖励坑里呢而无法跳出来的，而且使用课程学习去证明他就算弄出简单的题，这个有无机制能够使他真正能做出难题，而不是停留在简单题上？grpo确实是有可能提高pass@1但降低pass@k也即降低多样性的。问题就在于之前那些基座模型有可能被grpo又训坏掉了，现在让他弄多种变体的话，他这个变体能力又不够阿尔法proof它是有这个测试时更新的.在主更新环节，它应该是取了大量的问题，直接去丢到mcts里头再训.  然后测试时更新我记得是使用课程学习生成变体去训，然后我记得阿尔法狗zero，它是没有使用测试时更新的.他那个围棋网络好像就是进行一段时间的大规模self play，然后后面再冻结给别人调用，不过他应该也能够进行测试时更新。

Q3:我现在正在探索有没有高效的蒸馏方法.Pass@K其实对mcts是更重要的.能给他更多的探索和泛化能力。那就是工具调用阶段，它的工具调用状态更新的时候，从gpu端采样，它是似乎不是采样k格，是只采样一个，而是lean tactic状态更新的时候，它才会把它当做新节点参加K个。如果真的是这样子做下去的话，这是不是会导致在工具调用阶段要求pass@1能力提高，但是在lean的节点中，由于有mcts要求pass@k提高，但这样子是不是会矛盾？那工具调用阶段，你觉得要分叉去，采样多种吗？或者直接把普通工具调用也看作mcts节点？。如果工具调用状态也列入mcts节点的话，那价值头必须能够对这个也给出打分，但是这个似乎就不太符合lean tree对价值头的训练。不过，现在的传统的大模型放在智能体环境里头去书写lean证明的话，他也是经过多步工具调用的，因此可以让他们先解决比如说20k的数量的问题，然后用他的轨迹筛选其中那些成功解决了的，去对价值头做sft,就是让价值图预判还剩最少多少步能够证完.我的理念是，还是得维护某种状态机，而非token本身的价值.注意到，通常的传统大模型在智能体环境中，会有多轮的LLM API调用,也就是说，我们不会让 link state 1 变化就立马停止它此轮的 API 正常完成度,而是说，自然地等到下一次 open code 的新请求 LLM API 的时候,也就是说可能会有两个方案，方案1就是说此时，如果检测到 Lean 的证明状态发生变化，就从 GPU 端采样 K 种策略。方案 2 是：只要检测到 Lean 证明状态或工具调用的状态变化采样 K 种策****略，但是方案 2 就意味着每轮 API 调用几乎都要采样 K 种。因为工具调用如果你去维护一个状态，它是必然会变化的.所以我更倾向于方案一.方案一也只需要让人们给出lean证明状态变化上面的价值头的 SFT，而方案 2 似乎还需要带有工具调用的证明数的轨迹去sft价值头

如果使用方案一,伪代码:

根节点 s_0 = (C_0, Δ_0, E_0)
│
├─ [工具阶段] 确定性链，无分叉
│   s_0 → Exec_tool → s_0' → Exec_tool → s_0'' → ... → s_0^(L)
│   （每步更新 E，累积搜索结果、脚本输出、构建缓存）
│
├─ [tactic 采样] 在 s_0^(L) 处采样 m 个 tactic
│   ├─ a_1 → LeanApply → s_1  (决策节点)
│   ├─ a_2 → LeanApply → s_2  (决策节点)
│   └─ ...
│
└─ 对每个 s_j 递归：
若 s_j 需要工具 → 工具阶段（确定性链）
否则 → tactic 采样

Q4:如果遇到综合性很高的一些问题怎么办？他真的能够成功分解到不同领域吗？用严格数学语言，深入思考，仔细分析是否需要有新的训练方法**---
